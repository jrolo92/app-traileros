export interface Carrera {
        id: number;
        titulo: string;
        dificultad: string;
        descripcion: string;
        fecha: string;
        ubicacion: string;
        distanciaKm : number;
        desnivelPositivo: number;
        imagenUrl?: string;
}
